import styled from 'styled-components';
import { Button, IButton } from 'ui/Button';

export const StyledLoginButtonDev = styled(Button)<IButton>`
  position: fixed;
  right: 40px;
  bottom: 40px;
  z-index: 1000;
`;
