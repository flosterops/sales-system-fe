import styled from 'styled-components';
import { colors } from 'styles/colors';
import { Column } from 'ui/Layout';

export const AddOnsSectionWrapper = styled(Column)`
  background-color: ${colors.lightGray};
  border-radius: 10px;
`;
